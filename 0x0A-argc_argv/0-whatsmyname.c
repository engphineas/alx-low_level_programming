#include "main.h"
#include <stdio.h>

/**
 * main - programs that prints its name
 * @argc: count of arguments
 * @argv: vector of the arguments
 * Return: zero
 */

int main(int argc, char *argv[])
{
	(void) argc;
	printf("%s\n", argv[0]);
	return (0);
}
