#include "main.h"
/**
 * _isdigit - function to checks for a digit (0 to 9)
 * @c: int supplied to be checked
 * Return: 1 if a digit, 0 otherwise
 */
int _isdigit(int c)
{
	return (c >= '0' && c <= '9');
}
